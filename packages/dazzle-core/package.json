{
  "name": "dazzle-core",
  "version": "0.1.0",
  "description": "Core library for Dazzle: Scheme interpreter (ported from OpenJade) with bytecode VM",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "keywords": ["scheme", "interpreter", "dsssl", "openjade", "bytecode-vm"],
  "author": "Ralf Schleitzer <r.schleitzer@users.noreply.github.com>",
  "license": "MIT",
  "dependencies": {
    "libxmljs2": "^0.37.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
